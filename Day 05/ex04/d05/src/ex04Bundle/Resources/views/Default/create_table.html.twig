{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <h1>Create Table Result</h1>
    
    {% if success %}
        <div class="alert alert-success">
            <strong>Success!</strong> {{ message }}
        </div>
    {% else %}
        <div class="alert alert-danger">
            <strong>Error!</strong> {{ message }}
        </div>
    {% endif %}
    
    <div class="mt-3">
        <a href="{{ path('ex04_index') }}" class="btn btn-primary">Back to Home</a>
        <a href="{{ path('ex04_form') }}" class="btn btn-success">Add User</a>
        <a href="{{ path('ex04_list') }}" class="btn btn-info">View Users</a>
    </div>
</div>
{% endblock %}
