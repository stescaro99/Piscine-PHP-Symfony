{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <h1>Create Table Result</h1>
    
    {% if success %}
        <div class="alert alert-success">
            <strong>Success!</strong> {{ message }}
        </div>
    {% else %}
        <div class="alert alert-danger">
            <strong>Error!</strong> {{ message }}
        </div>
    {% endif %}
    
    <div class="mt-3">
        <a href="{{ path('ex10_index') }}" class="btn btn-primary">Back to Home</a>
        <a href="{{ path('ex10_form') }}" class="btn btn-success">Add Person</a>
        <a href="{{ path('ex10_list') }}" class="btn btn-info">View Persons</a>
    </div>
</div>
{% endblock %}
