{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <h1>Exercise 02 - CRUD with SQL</h1>
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('ex09_create_table') }}" class="btn btn-success btn-block">Create Table</a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('ex09_form') }}" class="btn btn-primary btn-block">Add Person</a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('ex09_list') }}" class="btn btn-info btn-block">View Persons</a>
        </div>
        <div>
            <a href="{{ path('ex09_add_marital_status') }}">Add Marital Status Column</a>
        </div>
        <div>
            <a href="{{ path('ex09_create_related_entities') }}">Create Related Entities</a>
        </div>
        <div>
            <a href="{{ path('ex09_create_relationships') }}">Create Relationships</a>
        </div>
    </div>
    
    <div class="mt-4">
        <h3>Instructions:</h3>
        <ol>
            <li><strong>Create Table:</strong> Creates the persons_ex09 table if it doesn't exist</li>
            <li><strong>Add Person:</strong> Form to insert new persons with validation</li>
            <li><strong>View Persons:</strong> Display all persons in a table format</li>
        </ol>
    </div>
</div>
{% endblock %}
