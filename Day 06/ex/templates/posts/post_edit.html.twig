{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <h1>Edit Post</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}
    <div>
        <a href="{{ path('post_detail06', {'id': post.id}) }}">⤾ Back to post</a>
    </div>
{% endblock %}
