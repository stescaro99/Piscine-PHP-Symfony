<!DOCTYPE html>
<html>
<head>
    <title>E03 - Color Shades</title>
    <style>
        table { border-collapse: collapse; margin: 20px auto; }
        th, td { 
            width: 80px; 
            height: 40px; 
            border: 1px solid #333;
            text-align: center;
            vertical-align: middle;
            font-weight:bold;
        }
        th { background-color: #f0f0f0; font-weight: bold; }
    </style>
</head>
<body>
    <h1>Color Shades Table</h1>
    
    <table>
        <thead>
            <tr>
                <th>Row</th>
                {% for color in baseColors %}
                    <th>{{ color|title }}</th>
                {% endfor %}
            </tr>
        </thead>
        <tbody>
            {% for row in 0..numberOfColors-1 %}
                <tr>
                    <td>{{ row + 1 }}</td>
                    {% for color in baseColors %}
                        <td style="background-color: {{ colorShades[color][row] }}; color: white; text-shadow: 1px 1px 2px black, -1px -1px 2px black;">
                            {{ colorShades[color][row] }}
                        </td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>